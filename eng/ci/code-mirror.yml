trigger:
  branches:
    include:
    - dev
    - in-proc
    - release/4.*
    - release/inproc6/4.*
    - release/inproc8/4.*

resources:
  repositories:
  - repository: eng
    type: git
    name: engineering
    ref: refs/tags/release

extends:
  template: ci/code-mirror.yml@eng

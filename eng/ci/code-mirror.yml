trigger:
  branches:
    include:
    - dev
    - in-proc
    - release/4.*
    - release/in-proc/4.*

resources:
  repositories:
  - repository: eng
    type: git
    name: engineering
    ref: refs/tags/release

extends:
  template: ci/code-mirror.yml@eng

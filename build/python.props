<Project>

  <!-- Looking to update python worker package? It is now located in workers.props. -->
  <Target Name="PythonBadImport" BeforeTargets="Build">
    <Error Message="Do not import python.props. Import workers.props instead." />
  </Target>

</Project>

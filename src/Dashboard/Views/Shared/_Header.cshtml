<div class="navbar">
    <div class="navbar-inner">
        <div class="container">
            <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <ul class="nav pull-right">
                <li><a title="Log Out" href="@Url.Action("Logout", "Account")"><i class="icon-signout"></i><span class="hidden-phone"> Log Out</span></a></li>
            </ul>
            <p class="navbar-text pull-right visible-desktop">Server time: @DateTime.UtcNow</p>
            <a class="brand" href="@Url.Action("ListAllInstances", "Log")">SimpleBatch</a>
            <div class="nav-collapse collapse">
                <ul class="nav">
                    @Html.MenuLink("History", icon: "icon-cogs", action: "ListAllInstances", controller: "Log")
                    @Html.MenuLink("Function Definitions", icon: "icon-cogs", action: "ListAllFunctions", controller: "Home")
                    @Html.MenuLink("About", icon: "icon-home", action: "Index", controller: "Home")
                </ul>
            </div>
        </div>
    </div>
</div>